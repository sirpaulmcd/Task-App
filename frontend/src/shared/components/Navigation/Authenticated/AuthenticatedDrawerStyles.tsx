import { makeStyles } from "@material-ui/core/styles";

const useAuthenticatedDrawerStyles = makeStyles((theme) => ({
  authenticatedDrawer_list: {
    width: 200,
  },
}));

export default useAuthenticatedDrawerStyles;
