import { makeStyles } from "@material-ui/core/styles";

const useUnauthenticatedDrawerStyles = makeStyles((theme) => ({
  unauthenticatedDrawer_list: {
    width: 200,
  },
}));

export default useUnauthenticatedDrawerStyles;
